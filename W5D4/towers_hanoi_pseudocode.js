class TowersOfHanoi {
  constructor() {

  }

  run() {
    // until starting tower AND one other tower are empty
      // prompts player for a move (from tower) (to tower)
      // checks to see if valid move?
      // moves disc if valid
      //
  }

}
